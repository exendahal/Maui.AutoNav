namespace Maui.AutoNav.Tests.Fixtures.Other;

// Shares a simple name with Fixtures.ViewModels.SettingsViewModel but lives in the "wrong"
// namespace - used to prove the resolver prefers a same-namespace match.
public class SettingsViewModel;
